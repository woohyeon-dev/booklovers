import React from 'react';
import styled from 'styled-components';

const Register = () => {
  return <RegisterBox>Register</RegisterBox>;
};

const RegisterBox = styled.div``;

export default Register;
